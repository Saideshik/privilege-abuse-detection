[project]
name = "pad"
version = "0.1.0"
description = "Privilege Abuse Detection (PAD) - reference implementation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pad = "pad.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
